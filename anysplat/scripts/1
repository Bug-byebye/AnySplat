4.3 相机位姿估计 (Camera Pose Estimation)
4.3.1 评测设定 (Benchmark Setting)
为了客观评估现有方法与模型在非朗伯体场景下的相机定位能力，我们构建了一套标准化的位姿评测基准。
数据转换与适配 (Data Adaptation)：
我们将 NL3D 合成评测数据集转换为与 CO3D [1] 协议兼容的目录及标注格式。
原始场景的相机外参经坐标变换后，以 PyTorch3D 约定的旋转 ($R$) 与平移 ($T$) 形式存储于 Gzip 压缩的 JSON 序列标注中。
在数据准备阶段，我们保留了图像的原始分辨率，以确保后续推理阶段能灵活适配不同模型的输入预处理需求。
评测基准与协议 (Evaluation Protocol)：
我们选取了 AnySplat [2] 与 VGGT [3] 两种通用前馈式模型作为 Baseline。
视角采样 (View Sampling)： 
我们针对稀疏、中等和密集三类观测需求，分别设置了 $N \in \{10, 32, 64\}$ 三组独立实验。
对于每一组特定的 $N$，我们生成了统一的采样计划（Sampling Plan），确保不同基准模型在处理同一序列时，均输入完全一致的帧索引集合，从而实现跨模型的绝对公平对比。
坐标系标准化 (Coordinate Normalization)： 
为了在缺乏统一世界坐标系的预测结果与真值（GT）之间建立基准，我们遵循 legacy 评估策略：仅对真值相机轨迹进行标准化处理，即将其整体投影至以序列首帧相机为原点的局部坐标系中。
预测轨迹则维持其推理时的原始坐标输出，通过计算相对位姿误差来规避全局刚体变换的影响。
评估指标 (Metrics)：
我们采用位姿误差的曲线下面积 (AUC) 衡量估计精度。
相对位姿误差： 对于采样出的 $N$ 张图像，构造所有可能的帧对（Pairs），计算每对图像之间相对旋转和平移方向的角度误差（$\Delta R$ 与 $\Delta T$）。
误差代表值： 取每一对中旋转与平移误差的最大值作为该样本的综合标量误差。
AUC 汇总： 我们分别在 $5^\circ$、$10^\circ$、$20^\circ$ 和 $30^\circ$ 的阈值下统计误差分布，通过计算累积分布函数（CDF）得出各阈值下的 AUC。
宏平均统计： 最终指标首先在 NL3D 定义的材质类别（如 Conductor, Dielectric）内汇总，随后在所有类别间计算算术平均值（Macro-average），以全面反映模型在不同光学特性下的性能分布。