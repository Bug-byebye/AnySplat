experiment:
  pretrained_model_path: "pretrained_model"
  num_context: 64
  camera_id: "18"  # used for vr-nerf
  fisheye_camera_id: "4"  # used for vr-nerf fisheye images

  metrics_dir: "exp-results/ttt-exp/nvs_compare"
  image_root_dir: "exp-results/ttt-exp"

# cover vrnerf_sampler.yaml
dense_sparse:
  pool_size: 72
  pool_stride: 2
  seed: 123
  images_subdir: "images-jpeg-1k"